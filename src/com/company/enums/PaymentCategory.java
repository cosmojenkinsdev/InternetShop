package com.company.enums;

public enum PaymentCategory {
    /**
     * Выбрал enum, а не класс, т.к. решил не добавлять логику в категории,
     * а ограничиться лишь фактом того, что они есть
     */
    SUBSCRIPTION,
    ADS,
    TAXI,
    FOOD
}
